{
  "_comment": "Copy to polar.config.json. Secure mode uses Polar's public customer-portal endpoints, so NO secret token is needed — only your organization id. Use server 'sandbox' for testing, 'production' when live.",
  "organizationId": "00000000-0000-0000-0000-000000000000",
  "server": "production"
}
